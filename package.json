{
  "name": "rewardredeem",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tailwindcss build -i src/styles.css -o dist/styles.css",
    "watch": "npx tailwindcss -i src/styles.css -o dist/styles.css --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.4.13"
  },
  "dependencies": {
    "flowbite": "^2.5.2"
  }
}
